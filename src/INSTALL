INSTALL

Install instructions for get_fasta_info

Need to have autoconf and automake installed,
in addition to a C compiler and the make system.

Then:

    cd src
    aclocal
    autoconf
    automake --missing
    ./configure
    make
    make install
